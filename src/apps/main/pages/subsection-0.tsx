import { colors } from '../../../theme'

export default function subsection() {
    return (
        <div className="min-h-screen flex items-center justify-center" style={{ backgroundColor: colors.blueDark, color: colors.white }}>
            <h1 className="text-2xl sm:text-3xl md:text-4xl font-bold font-primary">Subsección 0</h1>
        </div>
    )
}